# Dockerfile for Web Chat Application
# This builds the Flask-based web interface for ngrok + MongoDB Atlas

# Use Python 3.9 as the base image
FROM python:3.9-slim

# Set working directory inside the container
WORKDIR /app

# Copy requirements file first (for Docker layer caching)
COPY requirements.txt .

# Install Python dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy application files
COPY app_with_auth.py .
COPY database.py .
COPY .env .

# Copy React build
COPY frontend/dist ./frontend/dist

# Expose port 8080 (HTTP for ngrok)
EXPOSE 8080

# Run the authenticated web application
CMD ["python", "app_with_auth.py"]
